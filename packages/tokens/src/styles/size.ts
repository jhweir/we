import { NamedSize } from '@we/types/design';

export const size: Record<NamedSize, string> = {
  xxs: 'var(--we-size-xxs)',
  xs: 'var(--we-size-xs)',
  sm: 'var(--we-size-sm)',
  md: 'var(--we-size-md)',
  lg: 'var(--we-size-lg)',
  xl: 'var(--we-size-xl)',
  xxl: 'var(--we-size-xxl)',
};

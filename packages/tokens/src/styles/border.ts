const width = 'var(--we-border-width)';

const radius = {
  base: 'var(--we-border-radius)',
  sm: 'var(--we-border-radius-sm)',
  md: 'var(--we-border-radius-md)',
  lg: 'var(--we-border-radius-lg)',
};

const color = {
  base: 'var(--we-border-color)',
  strong: 'var(--we-border-color-strong)',
};

export const border = { width, radius, color };

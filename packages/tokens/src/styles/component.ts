const scrollbar = {
  width: 'var(--we-scrollbar-width)',
  backgroundImage: 'var(--we-scrollbar-background-image)',
  background: 'var(--we-scrollbar-background)',
  cornerBackground: 'var(--we-scrollbar-corner-background)',
  thumbBoxShadow: 'var(--we-scrollbar-thumb-box-shadow)',
  thumbBorderRadius: 'var(--we-scrollbar-thumb-border-radius)',
  thumbBackground: 'var(--we-scrollbar-thumb-background)',
};

export const component = { scrollbar };

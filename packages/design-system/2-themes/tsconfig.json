{
  "extends": "../../../tsconfig.json",
  "compilerOptions": {
    "target": "ESNext",
    "outDir": "dist",
    "rootDir": "src",
    "isolatedModules": true,
    // The theme-selector guard reads the primitives' source from disk.
    "types": ["node"]
  },
  "include": ["src"]
}
